#!/usr/bin/env bash
# Copy to clipboard - works on Wayland (wl-copy) and macOS (pbcopy)

if command -v wl-copy &> /dev/null; then
    wl-copy
elif command -v pbcopy &> /dev/null; then
    pbcopy
else
    echo "Error: No clipboard command found (tried wl-copy, pbcopy)" >&2
    exit 1
fi
