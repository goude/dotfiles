<?xml version="1.0" encoding="UTF-8"?>
<openbox_config xmlns="http://openbox.org/3.4/rc">

  <!-- ===== MENU HOOK ===== -->
  <menu>
    <file>~/.config/labwc/menu.xml</file>
    <!-- optional: delay before hiding submenus (ms)
    <hideDelay>200</hideDelay>
    -->
  </menu>

  <regions>
    <region name="left-67">
      <x>0%</x>
      <y>0%</y>
      <width>60%</width>
      <height>100%</height>
    </region>

    <region name="right-33">
      <x>60%</x>
      <y>0%</y>
      <width>40%</width>
      <height>100%</height>
    </region>

    <region name="left-50">
      <x>0%</x>
      <y>0%</y>
      <width>50%</width>
      <height>100%</height>
    </region>

    <region name="right-50">
      <x>50%</x>
      <y>0%</y>
      <width>50%</width>
      <height>100%</height>
    </region>

    <region name="mid-80">
      <x>15%</x>
      <y>0%</y>
      <width>70%</width>
      <height>100%</height>
    </region>
  </regions>

  <!-- ===== THEME & DECOR ===== -->
  <theme>
    <name>PiXonyx</name>
    <!-- Rounded corners + shadows (theme must support these) -->
    <cornerRadius>10</cornerRadius>
    <keepBorder>yes</keepBorder>
    <dropShadows>yes</dropShadows>
    <dropShadowsOnTiled>yes</dropShadowsOnTiled>

    <!-- Fonts -->
    <font place="ActiveWindow">
      <name>Inter</name>
      <size>10</size>
      <weight>Medium</weight>
      <slant>Normal</slant>
    </font>

    <font place="InactiveWindow">
      <name>Inter</name>
      <size>10</size>
      <weight>Light</weight>
      <slant>Normal</slant>
    </font>

    <!-- Menu font matched to the waybar bar (Hack Nerd Font). -->
    <font place="MenuHeader">
      <name>Hack Nerd Font</name>
      <size>9</size>
      <weight>Medium</weight>
      <slant>Normal</slant>
    </font>

    <font place="MenuItem">
      <name>Hack Nerd Font</name>
      <size>9</size>
      <weight>Regular</weight>
      <slant>Normal</slant>
    </font>

    <titlebar>
      <showTitle>yes</showTitle>
      <maximizeMode>titlebar</maximizeMode>
    </titlebar>
  </theme>

  <!-- ===== FOCUS & PLACEMENT ===== -->
  <focus>
    <followMouse>yes</followMouse>
    <raiseOnFocus>no</raiseOnFocus>
    <focusLast>yes</focusLast>
  </focus>

  <placement>
    <policy>Smart</policy>
    <center>yes</center>
    <monitor>Primary</monitor>
  </placement>

  <!-- Subtle gaps -->
  <margins>
    <top>8</top>
    <bottom>8</bottom>
    <left>8</left>
    <right>8</right>
  </margins>

  <!-- ===== EDGE SNAP UX ===== -->
  <snapping>
    <range>12</range>
    <cornerRange>48</cornerRange>
    <topMaximize>yes</topMaximize>
    <notifyClient>edge</notifyClient>
    <overlay>
      <enabled>yes</enabled>
    </overlay>
  </snapping>

  <!-- ===== MOUSE ===== -->
  <mouse>
    <!-- Right-click desktop = root menu -->
    <context name="Root">
      <mousebind button="Right" action="Press">
        <action name="ShowMenu">
          <menu>root-menu</menu>
        </action>
      </mousebind>
      <mousebind button="Middle" action="Press">
        <action name="ShowMenu">
          <menu>root-menu</menu>
        </action>
      </mousebind>
    </context>
  </mouse>

  <!-- ===== KEYBOARD (no keypad needed) ===== -->
  <keyboard>
    <default/>

    <keybind key="Super_L"/>
    <keybind key="Super_R"/>

    <!-- Launchers -->
    <!-- Open a 🐈 kitty Terminal -->
    <keybind key="W-Return">
      <action name="Execute">
        <command>kitty</command>
      </action>
    </keybind>
    <!-- Open a 👻 ghostty Terminal (complement to kitty) -->
    <keybind key="W-S-Return">
      <action name="Execute">
        <command>ghostty --gtk-single-instance=true</command>
      </action>
    </keybind>

    <!-- Window lifecycle -->

    <!-- 😷 Close Window -->
    <keybind key="W-Q">
      <action name="Close"/>
    </keybind>

    <!-- Toggle fullscreen -->
    <keybind key="W-F">
      <action name="ToggleFullscreen"/>
    </keybind>

    <!-- Maximize window -->
    <keybind key="W-M">
      <action name="Maximize"/>
    </keybind>

    <!-- Unmaximize window -->
    <keybind key="W-Shift-M">
      <action name="UnMaximize"/>
    </keybind>

    <!-- Split windows 2/3 left -->
    <keybind key="W-S-Left">
      <action name="Foreach">
        <query>
          <desktop>current</desktop>
        </query>
        <then>
          <action name="If">
            <query>
              <focused>yes</focused>
            </query>
            <then>
              <action name="SnapToRegion" region="left-67" />
            </then>
            <else>
              <action name="SnapToRegion" region="right-33"/>
            </else>
          </action>
        </then>
        <else></else>
      </action>
    </keybind>

    <!-- Split windows 1/3 right -->
    <keybind key="W-S-Right">
      <action name="Foreach">
        <query>
          <desktop>current</desktop>
        </query>
        <then>
          <action name="If">
            <query>
              <focused>yes</focused>
            </query>
            <then>
              <action name="SnapToRegion" region="right-33" />
            </then>
            <else>
              <action name="SnapToRegion" region="left-67"/>
            </else>
          </action>
        </then>
        <else></else>
      </action>
    </keybind>

    <!-- Split windows 50/50 -->
    <keybind key="W-S-Down">
      <action name="Foreach">
        <query>
          <desktop>current</desktop>
        </query>
        <then>
          <action name="If">
            <query>
              <focused>yes</focused>
            </query>
            <then>
              <action name="SnapToRegion" region="left-50" />
            </then>
            <else>
              <action name="SnapToRegion" region="right-50"/>
            </else>
          </action>
        </then>
      </action>
    </keybind>

    <!-- Toggle fullscreen -->
    <keybind key="W-S-Up">
      <action name="ToggleFullscreen" />
    </keybind>

    <!-- Half-tiling left -->
    <keybind key="W-Left">
      <action name="SnapToEdge" direction="left"/>
    </keybind>

    <!-- Half-tiling right -->
    <keybind key="W-Right">
      <action name="SnapToEdge" direction="right"/>
    </keybind>

    <keybind key="W-Up">
      <action name="Maximize"/>
    </keybind>

    <!-- Middle of screen -->
    <keybind key="W-Down">
      <action name="SnapToRegion" region="mid-80"/>
    </keybind>

    <!-- Show desktop 1 -->
    <keybind key="W-1">
      <action name="GoToDesktop"><to>1</to></action>
    </keybind>

    <!-- Show desktop 2 -->
    <keybind key="W-2">
      <action name="GoToDesktop"><to>2</to></action>
    </keybind>

    <!-- Show desktop 3 -->
    <keybind key="W-3">
      <action name="GoToDesktop"><to>3</to></action>
    </keybind>

    <!-- Previous / next desktop (PgUp / PgDn). GoToDesktop only accepts
         left/right/last/etc. - "previous"/"next" were invalid (the old bug). -->
    <keybind key="W-Prior">
      <action name="GoToDesktop"><to>left</to></action>
    </keybind>

    <keybind key="W-Next">
      <action name="GoToDesktop"><to>right</to></action>
    </keybind>

    <keybind key="W-S-1">
      <action name="SendToDesktop"><to>1</to><follow>yes</follow></action>
    </keybind>
    <keybind key="W-S-2">
      <action name="SendToDesktop"><to>2</to><follow>yes</follow></action>
    </keybind>
    <keybind key="W-S-3">
      <action name="SendToDesktop"><to>3</to><follow>yes</follow></action>
    </keybind>

    <!-- Screenshots -->
    <keybind key="Print">
      <action name="Execute">
        <command>grim "$HOME/Pictures/screenshot-$(date +%F_%H-%M-%S).png"</command>
      </action>
    </keybind>

    <!-- macOS-style screenshots (Super instead of Cmd).
         W-S-3 is taken (send-to-desktop), so full-screen-to-file stays on Print. -->
    <!-- Cmd-Shift-4: drag a region, save to file -->
    <keybind key="W-S-4">
      <action name="Execute">
        <command>sh -c 'grim -g "$(slurp)" "$HOME/Pictures/screenshot-$(date +%F_%H-%M-%S).png"'</command>
      </action>
    </keybind>
    <!-- Cmd-Ctrl-Shift-4: drag a region, copy to clipboard -->
    <keybind key="W-C-S-4">
      <action name="Execute">
        <command>sh -c 'grim -g "$(slurp)" - | wl-copy'</command>
      </action>
    </keybind>
    <!-- Cmd-Shift-5: drag a region, open in swappy to annotate/save -->
    <keybind key="W-S-5">
      <action name="Execute">
        <command>sh -c 'grim -g "$(slurp)" - | swappy -f -'</command>
      </action>
    </keybind>
    <!-- Cmd-Ctrl-Shift-3 analog: full screen to clipboard -->
    <keybind key="W-C-Print">
      <action name="Execute">
        <command>sh -c 'grim - | wl-copy'</command>
      </action>
    </keybind>

    <!-- Reconfigure labwc -->
    <keybind key="W-Escape">
      <action name="Reconfigure"/>
    </keybind>

    <!-- App launcher -->
    <keybind key="W-Space">
      <action name="Execute"><command>wofi --show drun</command></action>
    </keybind>

    <!-- Cycle through all windows on current desktop -->
    <keybind key="A-Tab">
      <action name="NextWindow">
        <allDesktops>yes</allDesktops>
        <panels>no</panels>
      </action>
    </keybind>

    <!-- Cycle through windows, reverse order -->
    <keybind key="A-S-Tab">
      <action name="PreviousWindow">
        <allDesktops>yes</allDesktops>
        <panels>no</panels>
      </action>
    </keybind>
  </keyboard>

  <!-- ===== DESKTOPS ===== -->
  <desktops>
    <number>3</number>
    <firstdesk>1</firstdesk>
    <names>
      <name>main</name>
      <name>web</name>
      <name>aux</name>
    </names>
    <popupTime>900</popupTime>
  </desktops>

  <!-- ===== WINDOW RULES ===== -->
  <applications>
    <!-- Center dialogs -->
    <application type="dialog">
      <center>yes</center>
      <decor>yes</decor>
    </application>
  </applications>

  <!-- ===== WACOM PEN ===== -->
  <!-- This pen (Wacom One) has a SINGLE physical button, which emits Stylus2
       (BTN_STYLUS2), so it is mapped to right-click. The tip is left-click and
       is not remappable. labwc tablet maps are flat: there is no way to gate a
       button on a modifier, so middle-click is not bound on the pen (use the
       keyboard/mouse for middle-click). -->
  <tablet mouseEmulation="no">
    <map button="Stylus2" to="Right"/>
  </tablet>

</openbox_config>

